# Core ML / signal computation
torch>=2.2
sentence-transformers>=2.7
spacy>=3.7
transformers>=4.40
sentencepiece>=0.2   # DeBERTa-v3 NLI tokenizer (fast tokenizer fallback)

# Layer 3 explainer + instruction-adherence judge
anthropic>=0.40
openai>=1.40

# Numerics
numpy>=1.26

# Web dashboard (Step 6)
fastapi>=0.110
uvicorn[standard]>=0.29

# PostgreSQL support (optional — falls back to SQLite/JSON when DATABASE_URL unset)
psycopg2-binary>=2.9
sqlalchemy>=2.0

# Per-user API key encryption
cryptography>=42.0

# Transactional email (optional — set RESEND_API_KEY to enable)
resend>=2.0

# Dev / test
pytest>=8.0
